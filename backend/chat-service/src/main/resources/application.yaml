spring:
  data:
    mongodb:
      uri: mongodb://${MONGO_USER:admin}:${MONGO_PASS:secret}@${MONGO_HOST:localhost}:27017/chat_db?authSource=admin

server:
  port: 8084

jwt:
  secret: ${JWT_SECRET:polyparrotpolyparrotpolyparrotpolyparrot}

internal:
  secret: ${INTERNAL_SECRET:polyparrot-internal-dev-2026}